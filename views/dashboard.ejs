<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
<header>
    <nav class="navbar navbar-dark bg-dark navbar-expand-md">
        <div class="container-fluid">
            <a class="navbar-brand" href="/logout">Déconnexion</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item text-uppercase"><a class="nav-link" href="/views/CRUD-catways.ejs">Catways</a></li>
                <li class="nav-item text-uppercase"><a class="nav-link" href="/views/CRUD-reservation.ejs">Réservations</a></li>
                <li class="nav-item text-uppercase"><a class="nav-link" href="/views/CRUD-users.ejs">Utilisateurs</a></li>
              </ul>
            </div>
        </div>
    </nav>
</header>

<main class="container my-4">
    <h1>Bienvenue, <%= user ? user.prenom : '' %> <%= user ? user.nom : '' %></h1>
    <p>Email : <%= user ? user.email : '' %></p>
    <p>Date du jour : <%= dateJour %></p>

    <h2>Liste des réservations pour la croisière</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Numéro de réservation</th>
                <th>Nom du passager</th>
                <th>Date de réservation</th>
                <th>Destination</th>
                <th>Statut</th>
            </tr>
        </thead>
        <tbody>
            <% reservations.forEach(function(reservation) { %>
                <tr>
                    <td><%= reservation.numero %></td>
                    <td><%= reservation.nomPassager %></td>
                    <td><%= new Date(reservation.dateReservation).toLocaleDateString() %></td>
                    <td><%= reservation.destination %></td>
                    <td><%= reservation.statut %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>